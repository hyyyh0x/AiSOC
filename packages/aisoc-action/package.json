{
  "name": "@aisoc/action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action that triages a repo's own security signals (Dependabot, CodeQL, secret scanning) with the AiSOC deterministic verdict engine.",
  "license": "MIT",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@aisoc/report-card": "workspace:*",
    "@types/node": "^25.9.2",
    "tsup": "^8.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
